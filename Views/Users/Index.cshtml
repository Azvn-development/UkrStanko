@model UkrStanko.ViewModels.Users.IndexViewModel

@{
    ViewBag.Title = "Пользователи";

    ViewBag.AddButton = true;
    ViewBag.Search = true;
}

@await Html.PartialAsync("~/Views/Shared/_Controls.cshtml")

<div class="data" id="data">
    @await Html.PartialAsync("_Table", Model.Users)
</div>

<script>
    $(window).scrollTop(0);

    checkCurrentUrl('@Url.Action("Index")');
</script>
