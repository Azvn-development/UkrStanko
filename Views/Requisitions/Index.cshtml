@model UkrStanko.ViewModels.Requisitions.IndexViewModel

@{
    ViewBag.Title = "Заявки";

    ViewBag.AddButton = true;
    ViewBag.Filters = true;
    ViewBag.Search = true;
}

@await Html.PartialAsync("~/Views/Shared/_Controls.cshtml")

<div class="data" id="data">
    @await Html.PartialAsync("_Table", Model.Requisitions)
</div>

<script>
    $(window).scrollTop(0);

    checkCurrentUrl('@Url.Action("Index")');
</script>